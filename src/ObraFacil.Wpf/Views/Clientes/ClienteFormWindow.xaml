<Window x:Class="ObraFacil.Wpf.Views.Clientes.ClienteFormWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cliente" Height="440" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding Titulo}"
                   Style="{StaticResource LabelDestaque}" FontSize="16" Margin="0,0,0,16"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Nome *"      VerticalAlignment="Center"/>
        <TextBox   Grid.Row="1" Grid.Column="1" Text="{Binding Nome, UpdateSourceTrigger=PropertyChanged}" Margin="0,4"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Telefone"    VerticalAlignment="Center"/>
        <TextBox   Grid.Row="2" Grid.Column="1" Text="{Binding Telefone}" Margin="0,4"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Text="E-mail"      VerticalAlignment="Center"/>
        <TextBox   Grid.Row="3" Grid.Column="1" Text="{Binding Email}" Margin="0,4"/>

        <TextBlock Grid.Row="4" Grid.Column="0" Text="CPF/CNPJ"    VerticalAlignment="Center"/>
        <TextBox   Grid.Row="4" Grid.Column="1" Text="{Binding Documento}" Margin="0,4"/>

        <TextBlock Grid.Row="5" Grid.Column="0" Text="Endereço"    VerticalAlignment="Center"/>
        <TextBox   Grid.Row="5" Grid.Column="1" Text="{Binding Endereco}" Margin="0,4"/>

        <TextBlock Grid.Row="6" Grid.Column="0" Text="Observações" VerticalAlignment="Top" Margin="0,8,0,0"/>
        <TextBox   Grid.Row="6" Grid.Column="1" Text="{Binding Observacoes}" Height="60"
                   TextWrapping="Wrap" AcceptsReturn="True" Margin="0,4"/>

        <!-- Botões -->
        <StackPanel Grid.Row="8" Grid.ColumnSpan="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancelar" Style="{StaticResource BtnSecondary}" Margin="0,0,8,0"
                    Click="BtnCancelar_Click" />
            <Button Content="Salvar"   Style="{StaticResource BtnPrimary}"
                    Click="BtnSalvar_Click" />
        </StackPanel>
    </Grid>
</Window>
